<resources>
    <!--
      System bar colors are handled at runtime via enableEdgeToEdge() in
      MainActivity and statusBarColor/navigationBarColor are deprecated /
      ignored on API 35+, so this style only needs the parent declaration.
    -->
    <style name="Theme.FalloutTerminalHacker" parent="android:Theme.Material.NoActionBar" />
</resources>
